import { Box } from '@mui/material';

import styles from './Documents.module.scss';

const DocumentsPage = () => {
  return <Box sx={{ p: 2 }}>Документы</Box>;
};

export default DocumentsPage;
